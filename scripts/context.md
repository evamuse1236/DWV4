# Curriculum Mapping Context

Source of truth: `playlist_mapping.json`.

Pipeline summary:
1. Repair/normalize mapping
2. Validate quality contract
3. Generate seed block
4. Apply seed block to `convex/seed.ts`
5. Run Convex seed mutations

Canonical workflow doc: `docs/curriculum/WORKFLOW.md`.
